Here's the notebook — already tested against your real Day 1 canonical cases and both sample PDFs rendered clean (I caught and fixed one bug: a warning emoji that showed up as a black box in the base PDF font, replaced with plain text). Upload your two heatmap PNGs into the Colab session before running the test cell, and section 7 has the exact edge changes to wire it into your existing graph.